# HANDOFF — 2026-07-29 (post June-validation meeting)

Written for the agent picking this up. Read `CLAUDE.md` and `PROJECT_STATUS.md` §0
first (client-confirmed rules you must NOT re-litigate), then this file.

Everything below is **verified against live prod or the workbooks at the time of
writing**, not inferred. Where something is unverified I say so explicitly.

---

## 1. State of the world (verified 2026-07-29)

| | |
|---|---|
| `main` | `38598fc`, pushed, **0 commits ahead of origin** |
| Backend | 271 tests pass; ruff + mypy clean |
| Frontend | 61 tests pass; eslint + tsc clean; build OK |
| Deployed | backend **and** frontend live; local build hash `index-k5sXq9Xg.js` == deployed hash (nothing unshipped) |
| Dirty tree | only `reference/meeting_transcript.MD` (overwritten with the 2026-07-28 meeting; the older one it replaced is in git history at `8941598`) |

**June ties the client's numbers.** Read out loud in the meeting and confirmed by
Renata line by line:

```
Institucional  custo equipe 210.781,04   despesas 105.932,16   impostos 39.753
Contencioso    custo 75.424,21  desp equipe 2.442  desp inst 32.564  RB 74.050
Econômico      custo 80.536,85  comissão 436  desp inst 34.770
Arbitragem     custo 54.383,94  desp equipe 11.598  desp inst 23.478
Reserva de bônus  −9.956,44        (workbook −9.955,93)
```

Adriana: *"Fernando espetacular"* / *"Arrebentou."* Do not "fix" these.

---

## 2. ⚠ Two questions that are ALREADY ANSWERED — do not ask again

I nearly wasted the client's time on both. The answers are in
`reference/meeting_transcript.MD`.

**The 4,80 bank tariff (`020.070.0030`) is settled. OUR NUMBER IS RIGHT.**
Adriana, 12:33: *"É a taxa bancária, a gente está zerado no Excel"* — finance does
not post bank tariffs at all — and *"o seu obviamente está muito mais certo porque
você tá puxando direto do sistema... essas diferenças de ajuste de 4, 5 reais dá
para a gente passar."* So our despesas 105.932,16 is correct and the workbook's
105.927,36 is the one missing 4,80. **Do not change the `020.070.` → Administrativas
mapping, and do not raise it again.** (The 51-centavo residual on reserva is this
same 4,80 × 10%.)

**Reforma (`040.030.0030`) → Despesas Gerais. Confirmed, already shipped** in
`79e9cb3`. Adriana: *"Joga para despesa geral, pronto. Não vai ter investimento
agora."* Adriana Mendes added that a genuine improvement-reforma would later get its
own separate account — so if a large reforma appears, flag it rather than silently
folding it in.

---

## 3. What was done this session (with the reasoning that matters)

### 3.1 Vale-ADM double count — the big one
`vale_adm` sums the transitória `200.010.0010` VR/VT-Mensal lines. In some months
that total **also contains the LAWYERS' Vale**, which is already counted in per-área
Custo equipe via `custo_equipe_area` (`500.010.<SIGLA>`). Counting it twice inflated
Despesa Institucional and every área's rateio share — June showed reserva
**−10.194,80** instead of −9.956,44.

**Two bookkeeping shapes exist. This is the crux — don't collapse them:**
- **May**: ONE lump pair on the transitória (VR 2.719,90 + VT 607,04 = 3.326,94), no
  `500.010.*` counterpart. All ADM. Ties workbook G122+G123.
- **June**: the transitória MIRRORS per-person `500.010.<SIGLA>` lines —
  JVO 1.283,00 + VSR 1.100,60 (lawyers) + **MLA 1.333,12 (ADM)** = 3.716,72, and the
  workbook types only 1.333,12.

Fixed in `extract.sql` by excluding rows having a same-month `500.010.*` twin
(structural test, not arithmetic).

**⚠ I tried the arithmetic shortcut (subtract Σ `custo_equipe_area`) and it is WRONG.
Measured across months, extract − lawyers vs the workbook's typed Vale-ADM:**
```
Jan 1.092,44 vs 1.127,96  X      Abr     0,00 vs 3.421,36  X
Fev 1.351,88 vs 1.351,88  OK     Mai 2.014,44 vs 3.326,94  X
Mar 1.240,92 vs 3.983,22  X
```
Only February. **Do not reintroduce that shortcut** — May's `test_vale_adm_ties_
salarios_administracao_may` (12.344,91) is the tripwire that caught it.

### 3.2 Client-platform Assinatura reclass
The 10.340,35 *"Licenças para uso da plataforma de faturamento do cliente"* is booked
to `040.040.0030`, which our `040.040.` prefix folded into institutional Informática.
The workbook books it as *"Assinaturas - Arbitragem e Compliance"* (Base_Resultado
H127) inside **Despesas Área** — which is what makes H203 = 15.115,27 and therefore
the **"Despesa para ratear" pool H207 = 90.812,09** tie. Left in Informática it
starved that pool and inflated every área's Despesa Institucional.

Reclassed in `despesas_liquido` (→ `020.060.0010`) **and** in the extract's
`despesas_equipe_area` filter. **Keep those two filters in step.**

### 3.3 The per-área Despesa Institucional formula (was an "open client question", wasn't)
`Base_Resultado Mensal_V2`: **H198 − H203 = H207**, then split by custo-equipe share.
```
Despesa Institucional 105.927,36 − Despesas Área 15.115,27 = pool 90.812,09
shares 35,8574 / 38,2880 / 25,8546 %  →  32.562,84 / 34.770,11 / 23.479,14
```
The Orçado side already had this (`DESPESA_PARA_RATEAR`); only realizado didn't.
My earlier "~19k unallocated / different ratio basis" note was **wrong** — it came
from comparing the rateio *result* against the pool line, and from pre-Vale shares.

### 3.4 Latent bug worth knowing
A reclass into an account with **no GERENC `despesas_conta` row of its own** was
silently dropped from the section tree AND from `despesas_total` (starving the pool).
`dre.py` now folds those orphans in, taking the family from an explicit map — NOT
`section_for(None, conta)`, whose no-parent fallback is "Despesas Gerais" and
mis-filed the Assinatura on my first attempt.

### 3.5 `040.030.*` mapped per account
It's a MIXED bucket; the blanket `040.030.` → Consultoria prefix was wrong.
`0010` Consultoria → Consultoria · `0020` Móveis e Utensílios → Despesas Gerais
(*"Material de Escritório"*, H105) · `0030` Reforma → Despesas Gerais.
Nine of ten June institucional families now tie the book exactly.

### 3.6 Durability scaffolding (so this can't silently rot)
The incident's real lesson: **the deployed CODE was right and the DATA was stale**,
so nothing errored — June just showed a wrong number in a client meeting.
- `meta.extract_version` in `extract.sql`, now **2**. Bump when a change alters the
  MEANING of an emitted field. Missing key ⇒ v1.
- `/api/ingest/<mes>/summary` now returns `{"extract": {version, expected, stale}}`.
- `probe_vale_twin_allmonths.sql` verifies the twin rule against Jan–Jun in one run
  (block C = what the fixed extract will emit), with the workbook's typed Vale-ADM in
  the header to compare against. Lints clean.

### 3.7 UI
- **Presentation whitespace** (`cca0be9`): `.slide` had `aspect-ratio: 4/3` forcing a
  fixed height, and `.slide-foot` was absolutely positioned so 40px of bottom padding
  was reserved for it. Both fixed; measured in headless Chrome — dead space below the
  last child went **385px → 15px**. Capa keeps a **fixed 360px** min-height, NOT `vh`
  (46vh measured 1074px on a tall viewport and reintroduced the band). Print still
  emits exactly one landscape page per slide.
- **Reserva KPI now red** (`2441e59`): it was the only `KpiCard` missing `signed`.
- Areas Sintetico expand/collapse per block header shipped earlier (`17faa70`).

---

## 4. ⚠ Live data state — Jan–May are STALE

```
2026-01 … 2026-05   v1  stale=true    ← serving PRE-FIX numbers
2026-06             v2  stale=false   ← correct
```

June is correct because I **hand-patched** its stored snapshot (fetched from Supabase,
applied the two corrections the fixed `extract.sql` makes, re-uploaded via
`POST /api/ingest`, then marked `extract_version: 2`). That was a one-off to unblock a
meeting.

**The real fix is `ops/sisjuri-agent/backfill.ps1` on MBC-LDESK01** — I have no route
to that box. The agent self-updates from `main`, so it will pick up the fixed SQL.
**Run `probe_vale_twin_allmonths.sql` first**; any month whose block C doesn't match
the workbook's typed Vale-ADM is a question for Renata, **not** a formula to guess.

Do NOT blanket-subtract to "fix" Jan–May — see §3.1.

---

## 5. TODO, in the order I'd do it

### 5.1 Per-área Orçado Imposto + Amortização are blank (in progress, nothing written)
Adriana, 14:30: *"Por que não está puxando aqui o orçado? O orçado amortização e
impostos."* Confirmed present for Institucional, absent for all three áreas — in the
**Areas Sintetico tab AND the presentation** (30:30: *"aqui ó, também tá faltando
aqueles orçados de imposto"*). Start at `dre._per_area_orcado`, which currently emits
only Recebimento / Custo / DespEq / DespInst / RB — no `IMPOSTO`, no `AMORTIZACAO`.
Institucional gets them via `OrcadoDerived.from_budget`. Note the workbook's own
per-área rule: Imposto = 15% × área recebimento, Amortização = total × área
custo-equipe share (that's how the *realizado* side does it in `_area_rows`).

### 5.2 Contencioso + Arbitragem differ ~7k, Econômico ties  ← most interesting
Per-área **DRE YTD** on the presentation slides:
```
Contencioso  Desp Equipe  orç 25,9K  real 14,4K (we showed 12,3K)
             Desp Inst    orç 220,4K real 203,4K (we showed 205,2K)
Arbitragem   custo equipe 336,8 · desp equipe 21,9 · desp inst 152,7 — all differ
Econômico    ties
```
Both you and Adriana suspected a **cross-área leak**: *"pode ser de lá, veio para cá.
Uma dá uma área, outra dá outra"* — and you noted both gaps are ~7k. Two áreas wrong
with the third exact is a strong signature for a misrouted slice, so chase that before
anything else. Likely suspects: `home_area` / `rateio_grupo` assignment, or the
Arbitragem Assinatura slice (§3.2) landing in the wrong área's YTD.

### 5.3 Add a Despesa card to the institucional slide
29:39 — Adriana: *"deveria ter essa linha de despesa: faturamento, receita, despesa."*
Adriana Mendes: *"só diminuir o tamanho da caixinha que cabe mais outra: faturamento,
receita, despesas e resultados."* So a 4-card row incl. Despesas in
`PresentationPanel.SlideInstitucionalMes`. Note the per-área monthly slides
deliberately show **no** despesa line (28:14 — *"a gente só mostra receita e
resultado"*); despesa appears per área only in the **acumulado**.

### 5.4 Jan–Abr faturamento not filling in the institucional detail table
19:49 — *"ele não está puxando janeiro, fevereiro, março, abril no faturamento
embaixo."* Receita fills, faturamento doesn't. Probably a
`faturamento`-vs-`recebimento` key mismatch in the per-month detail assembly, not a
data gap (the acumulado has the months).

### 5.5 OPEN-MONTH view — a real product change, get it right
Adriana pushed back on the closed-month gate: *"Por que ele não é online? ... Não é um
fechamento mensal, mas para a gente aproveitar muito mais as informações."* Adriana
Mendes: *"Com isso a atualização vira diária?"* The extract **already runs daily at
06:00**, so this is a display rule, not a data problem.

⚠ This **contradicts** `CLAUDE.md`'s "Open/future months are rejected" and
`available.is_closeable`, which I previously defended as correct. The client has now
explicitly asked for the in-progress month. Implement it as an **explicit
open/partial mode** (clearly labelled "mês em aberto — parcial"), do NOT just delete
the gate: a partial month must never be presented as a closing. Update `CLAUDE.md` in
the same commit so the convention and the code stop disagreeing.

### 5.6 Map (don't fix) the Jan–Abr differences
You committed to *mapping* them and bringing questions to the meeting: *"vou mapear
essas coisas e mostrar para vocês."* Known: receita bruta Abril **−81 (theirs) vs −85
(ours)**, ~4k; March 312 vs 309; YTD resultado bruto ~8k. Renata warned some may be
legitimate start-of-year or quarterly items (*"o imposto tem o trimestral"*) — so
produce a per-line diff and a question list, don't silently converge. Note Jan–Apr
also serve v1 data (§4), so re-extract FIRST or you'll map artefacts.

### 5.7 Logo + palette, then per-user logins
Replace the "RUMO" text with the Rumo logo and carry the presentation palette into the
dashboard (37:09 — *"essas cores aqui que você usou nesse aqui, a gente pode replicar
lá para o dinâmico"*). **Blocked on Adriana sending the logo + palette.**
Then per-user logins with an admin UI to create/disable (4 sócios at MBC; 39:22 —
*"o certo é um login para cada um"*). A "cliente demonstração" with scrambled numbers
was explicitly deferred to a separate project (41:18).

---

## 6. Operational notes that will save you an hour

- **EasyPanel API is oRPC**: `POST /api/rpc/<ns>/<proc>`, body `{"json": …}`.
  NOT tRPC at `/api/trpc` (that path 400s on everything and produced two false
  conclusions in an earlier session). Build logs: `actions/listActions` →
  `actions/getAction` → `.log`. `ops/easypanel-deploy.sh <svc> logs` wraps this.
- **Deploy verification**: backend via the **public** `/openapi.json` (reflects
  deployed route signatures); frontend by comparing `assets/index-<hash>.js` against a
  local `VITE_API_URL=<prod> npm run build`.
- A deploy can return `ok` and still **fail the build afterwards** — read the output;
  the helper now dumps the build log and exits non-zero.
- `docs/SISJURI_DB.md` §"Known account facts" — read before writing any probe, and add
  new facts there in the same commit.
- Full detail on all of the above: `PROJECT_STATUS.md` top section + the commit
  messages for `f5fe22c`, `79e9cb3`, `38598fc`, `cca0be9`.

---

## 7. Meeting logistics

Next meeting **Wednesday 11h** (moved off Tuesday — Renata has a school commitment on
the 4th). Adriana Mendes targets delivery **Thursday**: *"estamos no final do final,
são pequenos ajustes."*
