# HANDOFF — 2026-07-13 (SISJURI live-data validation)

> Read `PROJECT_STATUS.md` §0 first (client-confirmed rules, DO NOT re-ask), then
> `docs/MEETING_2026-07-10.md` (authoritative DRE spec) and
> `docs/SISJURI_DB.md` ("Known account facts" + "Workbook targets").
>
> **Goal (confirmed 2026-07-13):** fully replicate the MBC closing workbook (all
> tabs, faithful) as a multi-tenant SaaS. Workbook `Fechamento MBC 05.2026.xlsx`
> is the source of truth; every displayed number must tie to it (hard rule: blank
> if it doesn't match). Data = LegalDesk (revenue) + SISJURI Oracle DB
> (costs/expenses). No Juritis API.

## What just happened

- Ran the SISJURI extract for **2026-05** on `MBC-LDESK01` and it uploaded to the
  backend (HTTP 200). The snapshot is in **Supabase** (`sisjuri_snapshots`,
  `client_id='mbc'`, `ano_mes='2026-05'`), NOT the filesystem (`USE_FAKE_REPO=0`).
- **Backend URL is `https://rumo-backend.xem1qi.easypanel.host`** (the run-agent on
  the box still has this hardcoded). The IP `187.127.29.178:3000` the operator
  pasted is the **Easypanel dashboard**, not the API. Secrets files were corrected
  to the easypanel host. `INGEST_TOKEN` and Supabase keys live in `backend/.env`
  and `ops/sisjuri-agent/ingest.local.secrets` (both gitignored).
- To pull the full payload for analysis (summary endpoint hides it):
  `curl -H "apikey: $SUPABASE_SERVICE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_KEY" "$SUPABASE_URL/rest/v1/sisjuri_snapshots?client_id=eq.mbc&ano_mes=eq.2026-05&select=payload"`

## MAJOR WIN — Custo equipe por área ties the workbook TO THE CENTAVO

Validated the real May snapshot against the workbook targets. Two fixes to the
per-area Custo equipe derivation make all three areas match exactly:

| área | antes (bug) | com correções | alvo wb |
|------|------------:|--------------:|--------:|
| Contencioso | 75 378,11 | **74 141,21** | 74 141,21 |
| Econômico | 80 971,54 | **79 436,24** | 79 436,24 |
| Arbitragem | 54 383,94 | **54 383,94** | 54 383,94 |

**FIX 1 — Do NOT add Vale (`custo_equipe_area`, the `500.010.<SIGLA>` postings)
to per-area Custo equipe.** Proof: JVO's Vale = 1 236,90 = exactly Contencioso's
old residual. Vale belongs to the transitória/Salários-ADM path (`200.010.0010`),
per the meeting — not to team cost. Today `dre.py` does
`all_rows = deriv_rows + custo_equipe_area` and folds Vale in; that line is the bug.

**FIX 2 — For convênio médico (`030.010.0110`), use the parsed "Parte MBC" value
from `convenio_memo`, NOT the gross posted amount.** Proof: substituting the
`convenio_memo.parsed_valor` for the `0110` rows collapses Econômico's +1 459,69
residual to 0,00. The extract already emits `convenio_memo` (sigla, parsed_valor,
raw_memo) precisely for this; the assembler currently ignores it.

After both fixes: custo equipe total = **207 961,39**, which + comissão 2 128,07 =
Custos Diretos **210 089,46** (workbook target exact).

## STILL MISSING / TO PROBE (in priority order)

1. **Comissão por área — `comissao_deriv` came back `null`.** The extract block
   returned null for May, so per-area Comissão (implied **2 128,07** for May:
   210 089,46 Custos Diretos − 207 961,39 custo equipe) is absent. PROBE: verify
   the `comissao_deriv` SQL (external `020.110.0010` by grupo + internal
   `030.010.0120` by `LANCPROFDEST`) actually returns rows for May; likely a
   JOIN/date filter dropping everything. See `probe_comissao.sql`.

2. **Bônus equipe (conta `150.000.0000`) — not in this snapshot.** The extract on
   the box is the OLDER version without the `bonus_equipe` block (added in commit
   `96a986f` but NOT yet pushed to `main`, so the RDP pull didn't get it). ACTION:
   push to `main`, re-pull `extract.sql` on the box, re-run. Then validate the
   "Bônus equipe" line in Base_Resultado (Distribuição de Lucros extras). NB the
   partner split (Ricardo/Aurélio/Daniel/Martim out of 150.000.0000) is POINT 17,
   RUMO's task — for now 150.* is everyone.

3. **Vale-ADM (`200.010.0010` transitória).** Confirmed it does NOT go to team
   cost (fix 1). It DOES belong to Salários Administração (institutional). PROBE:
   confirm the May Vale-ADM total from `200.010.0010` by histórico (VR/VT) ties
   the workbook Salários-Adm Vale lines. See `probe_vale_adm.sql` /
   `docs/SISJURI_DB.md` "Vale Refeição/Transporte source".

4. **Despesas institucionais residual +56,33** (derived 105 567,76 vs alvo
   105 511,43). Tiny; a sub-account rebucket. Reconcile per-account against the
   workbook Despesas Indiretas rows once the bigger items above are closed.

5. **DL extras** (Bônus equipe done via #2; DL excedente sócios / MV / Cacione):
   see `docs/MEETING_2026-07-10.md` §G. Discretionary; blank when absent.

## The two fixes are backend-only (dre.py) and safe to implement now

They are TDD-able against the May payload (save it as a fixture). The hard rule
already blanks these cells until the fix lands, so shipping the fix turns them
from blank to correct. Suggested: add `backend/tests/fixtures/sisjuri_2026_05.json`
(the real payload, minus any secrets — it has none) and lock the three area
targets.

## How to re-pull + re-run on the box (RDP)

```powershell
[Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
$env:SISJURI_PASSWORD = '<RGN pw>'
$env:INGEST_TOKEN = '<token in ingest.local.secrets>'
$env:INGEST_URL = 'https://rumo-backend.xem1qi.easypanel.host/api/ingest'
Invoke-WebRequest -UseBasicParsing "https://raw.githubusercontent.com/femito1/rumo/main/ops/sisjuri-agent/extract.sql?nocache=$(Get-Random)" -OutFile C:\temp\sisjuri\extract.sql
powershell -ExecutionPolicy Bypass -File C:\temp\sisjuri\run-agent.ps1 -AnoMes 2026-05 -IngestUrl $env:INGEST_URL
```

(Requires the commits with the `bonus_equipe` extract block + these handoff notes
to be **pushed to `main`** first — currently committed locally, not pushed.)
